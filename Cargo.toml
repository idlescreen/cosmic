[package]
name = "idle-applet"
version = "2.1.3"
edition = "2024"
license = "Apache-2.0"
description = "IdleScreen for COSMIC — product package (applet + pulls daemon and recommended savers)"
repository = "https://github.com/idlescreen/idle-cosmic"
publish = false

[[bin]]
name = "idlescreen-applet"
path = "src/main.rs"

[[bin]]
name = "trance-applet"
path = "src/main.rs"

[dependencies]
anyhow = "1"
i18n-embed-fl = "0.10"
rust-embed = "8.11"
tokio = { version = "1.52", features = ["full"] }
libc = "0.2"
idle-dbus = { path = "idle/crates/idle-dbus" }
idle-runner = { path = "idle/idle-runner" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[dependencies.i18n-embed]
version = "0.16"
features = ["fluent-system", "desktop-requester"]

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"
rev = "ef162b8e"
features = [
    "applet",
    "applet-token",
    "dbus-config",
    "multi-window",
    "tokio",
    "wayland",
    "winit",
]

# ---------------------------------------------------------------------------
# Product package: `app-cosmic`
#
#   sudo dnf install app-cosmic
#   sudo apt install app-cosmic
#
# Installs this COSMIC applet and depends on the idle engine daemon
# (`idle-daemon`), with recommended screensaver plugins (`idle-saver-*`).
# ---------------------------------------------------------------------------

[package.metadata.deb]
name = "idle-cosmic"
changelog = "debian/changelog"
copyright = "2026 IdleScreen"
maintainer = "IdleScreen <jerydleuck@gmail.com>"
extended-description = """IdleScreen product for COSMIC Desktop.
Installs the COSMIC panel applet and depends on idle-daemon."""
depends = "libc6, idle-daemon, idle-tui"
provides = "app-cosmic, idlescreen-applet, trance-applet, idlescreen-cosmic"
replaces = "app-cosmic, idlescreen-applet, trance-applet, idlescreen-cosmic"
conflicts = "app-cosmic (<< 2.1.0), idlescreen-applet (<< 2.1.0), trance-applet (<< 2.0.0)"
recommends = "idle-cli, idle-tui, idle-saver-beams, idle-saver-bursts, idle-saver-chaos, idle-saver-cosmos, idle-saver-glyphs, idle-saver-gnats, idle-saver-hearth, idle-saver-radar, idle-saver-ripple, idle-saver-storm"
enhances = "cosmic-session, cosmic-panel"
section = "utils"
priority = "optional"
assets = [
    ["target/release/idlescreen-applet", "usr/bin/idlescreen-applet", "755"],
    ["target/release/trance-applet", "usr/bin/trance-applet", "755"],
    ["resources/app.desktop", "usr/share/applications/io.github.idlescreen.CosmicApplet.desktop", "644"],
    ["resources/app.metainfo.xml", "usr/share/metainfo/io.github.idlescreen.CosmicApplet.metainfo.xml", "644"],
    ["resources/icon.png", "usr/share/pixmaps/io.github.idlescreen.CosmicApplet.png", "644"],
    ["idlescreen-applet.1", "usr/share/man/man1/idlescreen-applet.1", "644"]
]

[package.metadata.generate-rpm]
name = "idle-cosmic"
release = "1"
arch = "x86_64"
summary = "IdleScreen for COSMIC"
description = "IdleScreen product for COSMIC: panel applet, requires idle-daemon and idle-tui."
license = "Apache-2.0"
assets = [
    { source = "target/release/idlescreen-applet", dest = "/usr/bin/idlescreen-applet", mode = "755" },
    { source = "target/release/trance-applet", dest = "/usr/bin/trance-applet", mode = "755" },
    { source = "resources/app.desktop", dest = "/usr/share/applications/io.github.idlescreen.CosmicApplet.desktop", mode = "644" },
    { source = "resources/app.metainfo.xml", dest = "/usr/share/metainfo/io.github.idlescreen.CosmicApplet.metainfo.xml", mode = "644" },
    { source = "resources/icon.png", dest = "/usr/share/pixmaps/io.github.idlescreen.CosmicApplet.png", mode = "644" },
    { source = "idlescreen-applet.1", dest = "/usr/share/man/man1/idlescreen-applet.1", mode = "644" }
]

[package.metadata.generate-rpm.requires]
idle-daemon = "*"
idle-tui = "*"

[package.metadata.generate-rpm.recommends]
idle-cli = "*"
idle-tui = "*"
idle-saver-beams = "*"
idle-saver-bursts = "*"
idle-saver-chaos = "*"
idle-saver-cosmos = "*"
idle-saver-glyphs = "*"
idle-saver-gnats = "*"
idle-saver-hearth = "*"
idle-saver-radar = "*"
idle-saver-ripple = "*"
idle-saver-storm = "*"

[package.metadata.generate-rpm.provides]
app-cosmic = "*"
idlescreen-applet = "*"
trance-applet = "*"
idlescreen-cosmic = "*"

[package.metadata.generate-rpm.obsoletes]
app-cosmic = "*"
idlescreen-applet = "*"
trance-applet = "*"
idlescreen-cosmic = "*"

[package.metadata.generate-rpm.supplements]
cosmic-panel = "*"
